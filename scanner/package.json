{
  "name": "netsage-scanner",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "if not exist scan-results mkdir scan-results"
  },
  "keywords": [
    "scanner",
    "security",
    "netsage",
    "web-scanner"
  ],
  "author": "NetSage",
  "license": "ISC",
  "description": "A website security scanner that replaces the n8n workflow while maintaining compatibility with the existing NetSage backend.",
  "dependencies": {
    "axios": "^1.11.0",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "node-nmap": "^4.0.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
